import { AppShell } from '../src/components/AppShell';

export default function NotFound() {
  return (
    <AppShell>
      <main style={{ padding: '128px 24px', textAlign: 'center' }}>
        <h1>ไม่พบหน้าที่ต้องการ</h1>
      </main>
    </AppShell>
  );
}
